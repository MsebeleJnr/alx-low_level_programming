#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Done)
 */
int main(void)
{
	char g;

	char h;

	g = 'a';
	h = 'A';
	while
		(g <= 'z') {
			putchar(g);
			g++;


		}
	while
		(h <= 'Z') {
			putchar (h);
			h++;


		}
	putchar('\n');
	return (0);
}

